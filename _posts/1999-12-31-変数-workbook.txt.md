---
layout: post
category: 変数
title: "workbook.txt"
---

<small>この文章はAIで生成しています。誤りが含まれることがあります。</small>

このプログラムは、ExcelのVBA（Visual Basic for Applications）を使用して、指定された名前のワークブック（Workbook）を開き、その中のワークシート（Worksheet）にアクセスするためのコードです。このコードは、まず「Book1.xlsx」という名前のワークブックを開き、そのワークブック内の情報にアクセスできるように設定します。

```vb
Dim wb as WorkBook
Set wb = Workbooks("Book1.xlsx")
Dim wb As Worksheet: Set wb = Workbooks("Book1.xlsx")
```
