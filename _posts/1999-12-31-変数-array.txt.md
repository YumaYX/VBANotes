---
layout: post
category: 変数
title: "array.txt"
---

<small>この文章はAIで生成しています。誤りが含まれることがあります。</small>

このプログラムは、配列内の各要素を順番に取り出し、それぞれの要素をコンソールに出力するVBAのコードです。配列arrには文字列"a"、"b"、"c"が格納されており、For Eachループを使用してそれぞれの要素を変数fに代入し、Debug.Print文を使用してコンソールに出力しています。

```vb
Dim arr As Variant
Dim f As Variant

arr = Array("a", "b", "c")

For Each f In arr
    Debug.Print f
Next f
```
