---
layout: post
category: 変数
title: "array.txt"
---

<small>この文章はAIで生成しています。誤りが含まれることがあります。</small>

このプログラムは、Excel VBAを使用して配列内の各要素を順番に表示するコードです。配列`arr`には文字列"a"、"b"、"c"が格納されており、`For Each`ループを使用してそれぞれの要素を`f`変数に代入し、`Debug.Print`ステートメントを使用してコンソールに表示しています。このコードは、配列内のすべての要素を順番に処理するための基本的なループ構造を示しています。

```vb
Dim arr As Variant
Dim f As Variant

arr = Array("a", "b", "c")

For Each f In arr
    Debug.Print f
Next f
```
