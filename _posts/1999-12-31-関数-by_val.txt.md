---
layout: post
category: 関数
title: "by_val.txt"
---

<small>この文章はAIで生成しています。誤りが含まれることがあります。</small>

このプログラムは、`f1`という名前のSubプロシージャを定義しています。このSubプロシージャは、文字列型の引数`str`を受け取ります。そして、受け取った文字列`str`の値をデバッグウィンドウ（Debug.Print()）に出力します。このプログラムは、`f1` Subプロシージャを呼び出し、文字列 "hello" を引数として渡しています。つまり、"hello"という文字列がデバッグウィンドウに出力される処理を行います。このプログラムは非常にシンプルで、文字列を引数として受け取った後、その文字列の内容を画面に表示するだけの処理を行っています。


```vb
Sub f1 (ByVal str As String)
  'Debug.Print(str)
End Sub

Call f1("hello")
```
